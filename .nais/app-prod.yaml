apiVersion: nais.io/v1alpha1
kind: Application
metadata:
  name: okonomimodell-api
  namespace: team-oebs
  labels:
    team: team-oebs
spec:
  image: {{image}}
  port: 8080

  azure:
    application:
      enabled: true
      allowAllUsers: true
      tenant: nav.no

  prometheus:
    enabled: true
    path: /api/v1/actuator/prometheus

  liveness:
    path: /api/v1/actuator/health/liveness
    initialDelay: 10
    periodSeconds: 5
    failureThreshold: 3

  readiness:
    path: /api/v1/actuator/health/readiness
    initialDelay: 10
    periodSeconds: 5
    failureThreshold: 3

  resources:
    requests:
      cpu: 100m
      memory: 512Mi
    limits:
      memory: 1Gi

  replicas:
    min: 2
    max: 4
    cpuThresholdPercentage: 80

  ingresses:
    - https://okonomimodell-api.intern.nav.no

  accessPolicy:
    inbound:
      rules: []
    outbound:
      external: []
      # TODO: Add OeBS hostname here once brunnmursåpning is confirmed
      # external:
      #   - host: <oebs-prod-hostname>

  envFrom:
    - secret: okonomimodell-api-oebs-credentials

  env:
    - name: SPRING_PROFILES_ACTIVE
      value: prod
